package com.example.jawafai.navigation

/**
 * Contains constants for all navigation routes in the application
 */
object Routes {
    // Existing routes (assumed)
    const val LOGIN = "login"
    const val REGISTER = "register"
    const val HOME = "home"
    const val PROFILE = "profile"
    const val SETTINGS = "settings"

    // New route for persona settings
    const val PERSONA_SETTINGS = "settings/persona"
}
